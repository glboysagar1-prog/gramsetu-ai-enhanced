# 🏗️ GramSetu AI - Voice Complaint Module Architecture

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    GramSetu AI Platform                         │
│                 Voice Complaint Module (NEW)                    │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Citizens   │     │Field Workers │     │  Officers    │
│  (Mobile)    │────▶│   (Mobile)   │────▶│ (Dashboard)  │
└──────────────┘     └──────────────┘     └──────────────┘
       │                    │                      │
       │ Voice Input        │ Review               │ Analytics
       │ (11 Languages)     │ Assign               │ Reports
       ▼                    ▼                      ▼
┌─────────────────────────────────────────────────────────────────┐
│                     API Gateway (Flask)                         │
│                    Port: 5000 (HTTP/HTTPS)                      │
└─────────────────────────────────────────────────────────────────┘
       │
       ├─── /api/v1/voice/upload        (POST) ◀── NEW
       ├─── /api/v1/voice/languages     (GET)  ◀── NEW
       ├─── /api/v1/voice/test          (GET)  ◀── NEW
       ├─── /api/v1/complaints          (GET/POST)
       ├─── /api/v1/dashboard           (GET)
       └─── /health                     (GET)
       │
       ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Voice Processing Pipeline                     │
└─────────────────────────────────────────────────────────────────┘

  ┌────────────────────────────────────────────────────────┐
  │  Step 1: Audio Upload & Validation                     │
  │  ────────────────────────────────────────              │
  │  • File type check (WAV, MP3, OGG, etc.)              │
  │  • Size validation (max 10 MB)                         │
  │  • Duration check (1s - 5min)                          │
  │  • Save temporarily                                    │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 2: Audio Preprocessing                           │
  │  ─────────────────────────                             │
  │  • Convert to mono (1 channel)                         │
  │  • Resample to 16kHz                                   │
  │  • Normalize volume                                    │
  │  • Remove silence                                      │
  │  • Export as WAV                                       │
  │                                                        │
  │  Tool: pydub + FFmpeg                                 │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 3: Speech Recognition (ASR)                      │
  │  ──────────────────────────────                        │
  │  • Whisper model transcription                         │
  │  • Automatic language detection                        │
  │  • Generate confidence scores                          │
  │  • Extract text segments                               │
  │                                                        │
  │  Engine: OpenAI Whisper                               │
  │  Model: base (74M params)                             │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 4: Language Detection & Validation               │
  │  ────────────────────────────────────                  │
  │  • Detect language (fallback)                          │
  │  • Validate against supported languages                │
  │  • Map to language name                                │
  │                                                        │
  │  Tool: langdetect                                     │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 5: NLP Classification                            │
  │  ────────────────────────                              │
  │  A) Category Classification                            │
  │     • Zero-shot classification                         │
  │     • 9 categories (Water, Health, etc.)              │
  │     • Keyword matching (multilingual)                  │
  │                                                        │
  │  B) Urgency Detection                                  │
  │     • Keyword analysis                                 │
  │     • Pattern matching                                 │
  │     • Levels: Low, Medium, High                       │
  │                                                        │
  │  C) Keyword Extraction                                 │
  │     • Top-5 keywords                                   │
  │     • Language-specific                                │
  │                                                        │
  │  Engine: HuggingFace Transformers                     │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 6: Validation & Fraud Detection                  │
  │  ─────────────────────────────────                     │
  │  A) Context Validation                                 │
  │     • Check for governance relevance                   │
  │     • Spam pattern detection                           │
  │     • Minimum length check                             │
  │                                                        │
  │  B) Duplicate Detection                                │
  │     • Sentence embedding (MiniLM)                     │
  │     • Cosine similarity (threshold: 0.9)              │
  │     • Check last 30 days                              │
  │                                                        │
  │  Tool: Sentence Transformers                          │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 7: CRS (Citizen Rating System)                   │
  │  ────────────────────────────────                      │
  │  • Update citizen score                                │
  │  • Valid complaint: +2                                 │
  │  • Invalid complaint: -10                              │
  │  • Duplicate: -5                                       │
  │  • Range: 0-100                                        │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 8: Blockchain Audit                              │
  │  ──────────────────────                                │
  │  • Generate unique complaint ID                        │
  │  • SHA256 hash (text + timestamp)                     │
  │  • Tamper-proof logging                               │
  │  • Immutable record                                    │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 9: Database Storage                              │
  │  ──────────────────────                                │
  │  • Save to SQLite (upgradable to PostgreSQL)          │
  │  • Link to citizen record                             │
  │  • Store metadata (language, duration, etc.)          │
  │  • Create audit trail                                  │
  └────────────────────────────────────────────────────────┘
           │
           ▼
  ┌────────────────────────────────────────────────────────┐
  │  Step 10: Response Generation                          │
  │  ─────────────────────────                             │
  │  • Format JSON response                                │
  │  • Include all metadata                                │
  │  • Return to client                                    │
  │  • Cleanup temp files                                  │
  └────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     Data Storage Layer                          │
└─────────────────────────────────────────────────────────────────┘

  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
  │  complaints  │  │   citizens   │  │ assignments  │
  ├──────────────┤  ├──────────────┤  ├──────────────┤
  │ id (PK)      │  │ id (PK)      │  │ id (PK)      │
  │ text         │  │ crs_score    │  │ complaint_id │
  │ category     │  │ created_at   │  │ worker_id    │
  │ urgency      │  │ updated_at   │  │ assigned_at  │
  │ citizen_id   │  └──────────────┘  │ resolved_at  │
  │ language     │                    └──────────────┘
  │ hash         │
  │ timestamp    │
  │ is_valid     │
  │ is_duplicate │
  │ crs_score    │
  └──────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     AI/ML Models Layer                          │
└─────────────────────────────────────────────────────────────────┘

  ┌────────────────────┐  ┌────────────────────┐  ┌──────────────┐
  │  Whisper ASR       │  │  BART Zero-Shot    │  │  MiniLM      │
  ├────────────────────┤  ├────────────────────┤  ├──────────────┤
  │ Size: 74M params   │  │ Task: Classification│  │ Task: Embed  │
  │ Task: Transcription│  │ Categories: 9      │  │ Use: Dup Det │
  │ Languages: 99      │  │ Accuracy: ~90%     │  │ Threshold:0.9│
  │ Speed: ~3s/10s     │  └────────────────────┘  └──────────────┘
  └────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     Integration Points                          │
└─────────────────────────────────────────────────────────────────┘

  ┌────────────────┐       ┌────────────────┐       ┌─────────────┐
  │ Flutter App    │◀─────▶│  Flask API     │◀─────▶│ React Dash  │
  │                │ JSON  │                │ JSON  │             │
  │ • Record voice │       │ • Process ASR  │       │ • Analytics │
  │ • Upload audio │       │ • Classify     │       │ • Reports   │
  │ • Show status  │       │ • Store data   │       │ • Monitoring│
  └────────────────┘       └────────────────┘       └─────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                     Security & Compliance                       │
└─────────────────────────────────────────────────────────────────┘

  • File validation (type, size, duration)
  • Spam detection (pattern matching)
  • Duplicate prevention (similarity check)
  • CRS system (prevent abuse)
  • Blockchain audit (SHA256 hash)
  • Temp file cleanup (privacy)
  • HTTPS support (encryption)
  • Rate limiting (DoS prevention)

┌─────────────────────────────────────────────────────────────────┐
│                     Monitoring & Logging                        │
└─────────────────────────────────────────────────────────────────┘

  • Request logging (all API calls)
  • Transcription logging (text output)
  • Error tracking (exceptions)
  • Performance metrics (processing time)
  • Usage analytics (language distribution)
  • Audit trail (blockchain)

```

---

## Component Interactions

### 1. Voice Upload Flow

```
Citizen (Mobile) 
    │
    │ Records voice in Hindi
    │
    ▼
Flutter App
    │
    │ Saves audio file
    │
    ▼
HTTP POST /api/v1/voice/upload
    │
    │ multipart/form-data
    │ - audio: complaint.mp3
    │ - citizen_id: CIT001
    │ - language: hi
    │
    ▼
Flask API Server
    │
    │ Receives request
    │
    ▼
VoiceComplaintService
    │
    ├─▶ validate_audio_file()
    ├─▶ preprocess_audio()
    ├─▶ transcribe_audio()
    │
    ▼
Whisper Model
    │
    │ Transcribes to text
    │
    ▼
MultilingualClassifier
    │
    ├─▶ classify_category()
    ├─▶ detect_urgency()
    ├─▶ extract_keywords()
    │
    ▼
Validation Layer
    │
    ├─▶ validate_complaint_context()
    ├─▶ detect_duplicates()
    ├─▶ update_citizen_crs()
    │
    ▼
Database
    │
    │ Save complaint
    │
    ▼
JSON Response
    │
    │ Return to client
    │
    ▼
Flutter App
    │
    │ Show confirmation
    │
    ▼
Citizen
```

---

## Technology Stack Details

### Backend (Python)
```
Flask 2.3.3           → Web framework
Werkzeug 2.3.7        → WSGI utilities
Flask-CORS 4.0.0      → Cross-origin support
```

### AI/ML (Python)
```
openai-whisper        → Speech recognition
transformers 4.35.2   → NLP models
sentence-transformers → Embeddings
torch 2.1.1           → ML framework
scikit-learn 1.3.2    → ML utilities
```

### Audio Processing
```
pydub 0.25.1          → Audio manipulation
ffmpeg-python 0.2.0   → FFmpeg wrapper
SpeechRecognition     → Audio I/O
```

### Utilities
```
langdetect 1.0.9      → Language detection
numpy 1.24.3          → Numerical ops
requests 2.31.0       → HTTP client
```

---

## Deployment Architecture (Production)

```
┌─────────────────────────────────────────────────────────────────┐
│                       Load Balancer                             │
│                    (HTTPS/SSL Termination)                      │
└─────────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│  API Server  │      │  API Server  │      │  API Server  │
│   (Flask)    │      │   (Flask)    │      │   (Flask)    │
│   + Whisper  │      │   + Whisper  │      │   + Whisper  │
│              │      │              │      │              │
│  Port: 5000  │      │  Port: 5000  │      │  Port: 5000  │
└──────────────┘      └──────────────┘      └──────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   PostgreSQL     │
                    │   (Primary DB)   │
                    └──────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  Redis Cache     │
                    │  (Model Cache)   │
                    └──────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │  S3/Cloud        │
                    │  (Audio Storage) │
                    └──────────────────┘
```

---

## Performance Considerations

### Optimization Strategies

1. **Model Caching**
   - Load Whisper model once at startup
   - Keep in memory for subsequent requests
   - Lazy loading for faster startup

2. **Parallel Processing**
   - Use Celery for async tasks
   - Queue audio processing jobs
   - Return immediate response

3. **GPU Acceleration**
   - Use CUDA for 10x speed
   - Batch processing
   - Mixed precision (FP16)

4. **Caching**
   - Redis for frequent queries
   - Model output caching
   - API response caching

5. **Horizontal Scaling**
   - Multiple API servers
   - Load balancing
   - Auto-scaling based on load

---

## Security Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     Security Layers                             │
└─────────────────────────────────────────────────────────────────┘

Layer 1: Network Security
    ├─ HTTPS/TLS encryption
    ├─ Firewall rules
    ├─ DDoS protection
    └─ Rate limiting

Layer 2: Application Security
    ├─ Input validation (file type, size)
    ├─ Sanitization (prevent injection)
    ├─ Authentication (API keys)
    └─ Authorization (role-based)

Layer 3: Data Security
    ├─ Encryption at rest
    ├─ Encryption in transit
    ├─ Blockchain audit (SHA256)
    └─ Temp file cleanup

Layer 4: Fraud Detection
    ├─ Spam detection
    ├─ Duplicate detection
    ├─ CRS scoring
    └─ Pattern analysis
```

---

## Scalability Roadmap

### Phase 1: Current (MVP)
- Single server
- SQLite database
- Local file storage
- CPU-based processing

### Phase 2: Production (Recommended)
- Multiple API servers
- PostgreSQL database
- Cloud storage (S3)
- GPU acceleration

### Phase 3: Scale (Future)
- Kubernetes orchestration
- Microservices architecture
- Auto-scaling
- Real-time processing
- Multi-region deployment

---

**Architecture designed for:**
- ✅ Scalability (horizontal & vertical)
- ✅ Reliability (fault tolerance)
- ✅ Performance (optimized pipeline)
- ✅ Security (multi-layer protection)
- ✅ Maintainability (modular design)
